/** @type {import('next').NextConfig} */
const nextConfig = {
    i18n: {
      locales: ['sr', 'en'],
      defaultLocale: 'sr',
      localeDetection: true,
    },
  };
  
  module.exports = nextConfig;
  